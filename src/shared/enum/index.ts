export enum VeriffSessionStatus {
  CREATED = 1,
  STARTED = 2,
  SUBMITTED = 3,
  RESUBMISSION_REQUESTED = 4,
  DECLINED = 5,
  APPROVED = 6,
}

export enum VeriffSessionUserDocumentIdTypes {
  PASSPORT = 1,
  NATIONAL_IDENTITY_CARD = 2,
  RESIDENCE_PERMIT = 3,
}
